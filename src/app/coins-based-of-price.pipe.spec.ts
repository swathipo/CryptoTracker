import { CoinsBasedOfPricePipe } from './coins-based-of-price.pipe';

describe('CoinsBasedOfPricePipe', () => {
  it('create an instance', () => {
    const pipe = new CoinsBasedOfPricePipe();
    expect(pipe).toBeTruthy();
  });
});
